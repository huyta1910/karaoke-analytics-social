name: 'social_analytics'
version: '1.0.0'
config-version: 2

profile: 'social_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  # BigQuery settings
  bq_project: "caramel-spot-474502-p2"
  bq_dataset: "social_analytics"
